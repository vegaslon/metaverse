stages:
    - test

#cache:
#    paths:
#        - server/node_modules/

Server tests:
    image: node:latest
    stage: test
    script:
        - cd server
        - npm i
        - npm run test:cov
    coverage: /All files[^|]*\|[^|]*\s+([\d\.]+)/